export const SetPopUp = (payload)=>({
    type:'SET_POP_UP',
    mode:payload
})
export const SetLearnMore = (payload)=>({
    type:'SET_LEARN_MORE',
    mode:payload
})

export const SetCard = (payload)=>({
    type:'SET_CARD',
    mode:payload
})
